---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="404 — Page Not Found" description="The page you are looking for does not exist.">
  <div class="not-found-container">
    <div class="not-found-badge">404</div>
    <h1 class="not-found-title">Page Not Found</h1>
    <p class="not-found-text">
      The note or article you are trying to access has been moved, renamed, or no longer exists in this vault.
    </p>
    <div class="not-found-actions">
      <a href="/" class="btn-primary">Return to Home</a>
      <a href="/blog" class="btn-secondary">Browse All Articles</a>
    </div>
  </div>
</BaseLayout>

<style>
  .not-found-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 6rem 1rem;
  }

  .not-found-badge {
    font-family: var(--font-mono);
    font-size: 5rem;
    font-weight: 800;
    line-height: 1;
    color: var(--accent);
    margin-bottom: 1rem;
    letter-spacing: -0.05em;
  }

  .not-found-title {
    margin: 0 0 1rem 0;
    font-size: 2rem;
    letter-spacing: -0.02em;
  }

  .not-found-text {
    max-width: 480px;
    color: var(--text-muted);
    font-size: 1.1rem;
    line-height: 1.6;
    margin: 0 0 2.5rem 0;
  }

  .not-found-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    padding: 0.65rem 1.25rem;
    background-color: var(--accent);
    color: #ffffff;
    border-radius: 6px;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .btn-primary:hover {
    opacity: 0.9;
    color: #ffffff;
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    padding: 0.65rem 1.25rem;
    background-color: var(--bg-surface);
    color: var(--text);
    border: 1px solid var(--border);
    border-radius: 6px;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    transition: background-color 0.2s ease, border-color 0.2s ease;
  }

  .btn-secondary:hover {
    background-color: var(--bg-subtle);
    border-color: var(--accent);
  }
</style>
